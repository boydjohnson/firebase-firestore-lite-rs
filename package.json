{
  "name": "firebase-firestore-lite-rs",
  "version": "0.0.1",
  "description": "Rust bindings to firestore lite",
  "scripts": {
    "build": "parcel build index.html",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/boydjohnson/firebase-firestore-lite-rs.git"
  },
  "keywords": [
    "wasm-bindgen",
    "Rust",
    "firebase"
  ],
  "author": "",
  "license": "APACHE-2.0",
  "bugs": {
    "url": "https://github.com/boydjohnson/firebase-firestore-lite-rs/issues"
  },
  "homepage": "https://github.com/boydjohnson/firebase-firestore-lite-rs#readme",
  "dependencies": {
    "firebase": "^9.6.2"
  },
  "devDependencies": {
    "parcel": "^2.1.1"
  }
}
